import Foundation

enum Defaults {
    static let startTime = "startTime"
    static let intervalSeconds = "intervalSeconds"
    static let activeSession = "activeSession"
    static let lastAliveAt = "lastAliveAt"
    static let lastCategory = "lastCategory"
    static let vaultRoot = "vaultRoot"
    static let idleThresholdSeconds = "idleThresholdSeconds"
    static let breakThresholdSeconds = "breakThresholdSeconds"
}
